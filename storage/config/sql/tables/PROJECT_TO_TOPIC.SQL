CREATE TABLE PROJECT_TO_TOPIC(
    PROJECT_TO_TOPIC_SK INTEGER,
    PROJECT_SK INTEGER,
    TOPIC_SK INTEGER,
    PRIMARY KEY PROJECT_TO_TOPIC_SK,
    FOREIGN KEY (PROJECT_SK) REFERENCES PROJECT(PROJECT_SK),
    FOREIGN KEY (TOPIC_SK) REFERENCES TOPIC(TOPIC_SK)
)