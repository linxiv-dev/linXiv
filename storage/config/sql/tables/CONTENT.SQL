CREATE TABLE IF NOT EXISTS CONTENT(
    CONTENT_FK INTEGER NOT NULL,
    CONTENT_TEXT TEXT,
    CONTENT_FILE BLOB,
    CREATED_AT TEXT NOT NULL DEFAULT (date('now')),
    UPDATED_AT TEXT NOT NULL DEFAULT (date('now')),
    PRIMARY KEY (CONTENT_FK)
);
