CREATE TABLE PAPER_TO_AUTHOR(
    CONTENT_SK INTEGER NOT NULL,
    PAPER_SK INTEGER NOT NULL,
    CONTENT_TEXT varchar(50000) DEFAULT NULL,
    CONTENT_FILE LONGBLOB DEFAULT NULL,
    PRIMARY KEY CONTENT_SK
    FOREIGN KEY (PAPER_SK) REFERENCES PAPER(PAPER_SK)
);