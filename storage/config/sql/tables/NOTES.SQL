CREATE NOTES(
    NOTE_SK INTEGER,
    PAPER_SK INTEGER,
    PROJECT_SK INTEGER,
    NOTE LONGBLOB,
    PRIMARY KEY NOTE_SK
    FOREIGN KEY (PROJECT_SK) REFERENCES PROJECT(PROJECT_SK)
    FOREIGN KEY (PAPER_SK) REFERENCES PAPER(PROJECT_SK)
)